# Android Companion — Phase 2

No executable is implemented in Phase 1. The planned Kotlin/Compose companion will use a foreground service, permission-scoped notification/media APIs, Android Keystore identity, and a TLS WebSocket connection. Message replies will require visible confirmation unless the user creates an explicit safe rule.

